## Learned User Preferences

- Prefer workspace-scoped MCP config (project `.cursor/mcp.json`) over global always-on Cursor MCP for tools like TradingView
- When validating a strategy, prefer finding historical examples in TimescaleDB before live signal hunting or full-universe backtests
- Persist coverage inventories, strategy plans, and phase status under `docs/status_log/` (roll-up `current_status.md`; Weekly BigVol under `weekly_bigvol/`; edge hunt under `edge_hunt/`; channel-touch under `edge_hunt/channel_touch/`; day digests under `daily/`)
- For market-data coverage questions, focus on stocks and exclude options unless options are requested
- For long multi-symbol hunts or backtests, record wall-clock timings in the relevant status log
- For rare-signal strategies, assess with PF/expectancy/payoff/MDD per `docs/assessing_strategies.md`; do not treat win rate as the primary gate; channel-touch: unique symbol per calendar day (not RS top1 as a hard cap; `--max-entries-per-day 1` remains optional) and ATR hard-stop k≈2.0 clamped 1.5%–6% (tester −6.25% = 6% ceiling + 0.25% friction, not a 3% stop) — do not replace ATR keepers with a fixed 2% stop (PF can rise while expectancy/WR fall); soft-promote `--require-in-channel` + `--max-channel-span-days 365` + `--max-beyond-width 0.25` (0.0 too strict; 0.5/1.0 worse on IB-windowed stack); reject H3 lower-40% geometry, hard ADV/ATR floors, SPY SMA50 alone, structure-exit bundles, L3 shakeout rebuy, and ridge P&L confidence sizing on RS-top1 (nightly stays equal-dollar); prefer longer history than 2022-only windows (unwindowed full-panel scans miss early years; windowed 504/252 recovers pre-2020); treat the edge as fat-tailed (median trade can be negative while mean/PF stay positive) and stress with drop-top-N/winsor/bootstrap; portfolio max-concurrent is a live gap before capital sizing; l3_touch wick fills may only use completed bars before fill (15m `i-1` / `--feature-asof prior-bar`); do not promote fill-bar `min_close_loc` or daily `--intraday-fill 15m` until it beats wait-6/RSI-50 without the fill bar's close
- Edge-hunt promotion vs SPY is risk-adjusted: Sharpe > 1.0, Sharpe >= SPY, and MDD better than SPY (CAGR is reported, not the ranking objective)
- After Phase 5 stock-level signals and Phase 6c VIX overlays failed to clear or lift the gate, prefer freezing and productionizing Phase 6b near-KEEP `Blend_VT60_BV40` over more Sharpe>1 fishing on the same window
- Validate the BigVol backtester visually via TradingView entry/exit order markers on discrete sleeve trades before trusting vectorized fills
- Classical chart-pattern detectors must follow Edwards & Magee / Murphy / Bulkowski rules: ascending channel ≠ ascending triangle (flat top); require distinct swings with significant intervening moves, ≥2 confirmed opposite-boundary touches, and reject repeated support/resistance violations
- Prefer original Pine/Python pattern implementations over copying proprietary or CC BY-NC-SA chart-pattern libraries (e.g. Trendoscope ACP)
- For large-universe daily strategies (~2k symbols), monitor with a post-close EOD scan on stored bars; do not stream the full universe via IB — use IB only for execution and open positions; for a 15m channel-touch book, keep an armed-H2 watchlist, use Alpaca last-price as a proximity screen, and pull IB 15m bars only on the hot list at bar close (do not poll all ~1478 via IB every bar; last price is not a fill); track hot candidates on `charting_server.py` `/hot` (TimescaleDB, sort/filter by distance to resist, live last); 15m Telegram from the minute CronRunner (`utils/notify/telegram_pinger.py`, not TradingView alerts and not MCP); Windows jobs live in `crons/crontab.yaml` ticked by hidden task `backTraderTest\CronRunner` (`scripts/pipeline/cron_manager.py`) — do not add per-job Task Scheduler entries

## Learned Workspace Facts

- Stock OHLCV lives in TimescaleDB `market_data`; use `utils.db.timescaledb_client.get_timescaledb_client()`
- Daily (`1d`) coverage is about 2,217 symbols, mainly ALPACA; after the 2026-08-23 gap-fill most names reach ~2026-08-20/21 (~2109/2217 fresh); no weekly timeframe is stored (resample from daily)
- 15m coverage is about 1,478 symbols (~58.7M bars estimated), mainly IB, roughly 2018-01-02 to 2025-12-02 (catch-up via `scripts/data/backfill_ib_15m_universe.py`, client id 8822, resume-safe; overnight CronRunner job `backfill_ib_15m_universe` at 02:30); single-symbol remains `scripts/data/backfill_ib_15m_symbol.py` (client 8821; keep ids distinct from other IB hist sessions); QQQ/GLD/IWM/NVDA often still missing on IB 15m; ALPACA 15m is effectively unused aside from leftover `AEO`; for 15m universe backtests prefer the IB 15m symbol set over the full daily universe to avoid empty loads
- Heavy full-table `COUNT`/`GROUP BY` on `market_data` is slow and can hit shared-memory or disk pressure; prefer per-timeframe/provider queries and `EXPLAIN` estimates
- TradingView MCP (`tradesdontlie/tradingview-mcp`) is enabled only for this workspace via `.cursor/mcp.json`, with the server under the user home `tradingview-mcp` folder and CDP on port 9222; it exposes price/quote/indicator/chart control, not historical fundamentals/financials; Polygon Stocks Basic also excludes Financials & Ratios (paid add-on)
- Weekly BigVol + TTM Squeeze lives in `strategies/weekly_bigvol_ttm_squeeze.py` / `weekly_bigvol_components.py`; example hunter is `scripts/data/find_weekly_bigvol_examples.py`; multi-symbol loads use `utils/data/ohlcv_loader.py`; default research execution is no fixed TP + ~10% stop (plus MA exit); `scanner_runner.py --scanner squeeze` is TTM zero-cross only; W-FRI resample + squeeze heuristics can lag TradingView LazyBear flips by weeks, and split-unadjusted forward returns can distort outcomes
- Edge-hunt vectorized research lives in `utils/research/` and `scripts/research/` (not `strategies/`); status under `docs/status_log/edge_hunt/`; Phase 4 KEEP is `Blend_SPY70_BV30`; Phase 5 XS mom + swing MR failed; Phase 6 vol-target near-miss and CTA weak; Phase 6b near-KEEP is `Blend_VT60_BV40` (vol-target SPY + BigVol); Phase 6c VIX curve ran with no promote / no lift vs plain vol-target; BigVol sleeve trade export and TradingView marker plans live in `scripts/research/export_bigvol_trades.py` with CSVs/plans under `reports/edge_hunt_phase6b/trades/`
- ALPACA daily panel coverage for many names starts ~2020-2022 (wide panel early years mostly empty); research loads can stitch earlier history via `load_ohlcv_many(..., fallback_provider="IB", merge_mode="prefix")` (IB bars strictly before first Alpaca bar; parquet cache key `ALPACA_IB`); backfill those prefixes into TimescaleDB with `scripts/data/backfill_ib_daily_prefix.py` — IB hist pacing (not daily bar count) is the throughput bottleneck; compute SPY SMA on full IB SPY history then align to the panel index
- Macro ETFs for overlays (TLT/GLD/USO/UUP/IEF/DBC) were Alpaca-ingested for Phase 6 with history often from ~2020-07; IB can supply CBOE `VIX`/`VIX3M`/`VVIX` when Gateway farms (especially `ushmds`) are connected; Alpaca VIX ETF proxies (VIXY/VXX/etc.) are also available; VX futures history from IB was too thin to use
- `ticker_universe` holds about 6,072 tickers; current daily and 15m symbols are subsets of that universe
- Ascending-channel / channel-touch research: classical detector in `scripts/research/find_ascending_channels.py` with Pine `indicators/pine/ascending_channel_3touch.pine` (keep detector v1 unchanged; quality via post-filters, not rewrite); backtest `scripts/research/backtest_channel_touch_trades.py` (RS top1/day, optional TTM squeeze-adaptive wider trail, `--edge-v2` ATR-stop/geometry sweeps; soft-promote `--require-in-channel` + `--max-channel-span-days 365` + `--max-beyond-width 0.25`; `--entry-features` snapshots RSI/MA/RS/vol/squeeze/geometry at the last completed bar before a wick fill (`--feature-asof auto` = `prior-bar` for 15m/`--intraday-fill` `l3_touch`; pivot still uses the confirmation close) for univariate mining via `scripts/research/analyze_channel_touch_entry_features.py`; default start ~2018-11-01; default entry is pivot-confirm after `pivot_len` bars — trigger date/price is not the rail-touch bar (research/backtest default; live nightly is H2 resist-break); `--entry-mode l3_touch` arms at H2 print and fills the first from-above support tag + slip (reject gap-through/crash bars whose high never reaches support; cancel if a prior bar already closed below support or closed above resistance; abort the setup if support is tagged before `--min-l3-wait-bars` default research 6 — a 1-5 bar dip is not a swing L3; do not retarget a later dip); uses separate `find_h2_l3_setups` (keep `find_channels` v1 unchanged); l3_touch research stack `--min-l3-wait-bars 6 --max-rsi 50` plus in-channel/span365/beyond 0.25 beat pivot on E/PF in the IB-windowed 2018-2026 sample (retired as nightly 2026-08-29; rollback `--no-h2-resist-break --require-in-channel --max-rsi 50 --max-beyond-width 0.25`); H2 resist-break (`--h2-resist-break` / `scripts/research/backtest_channel_touch_h2_break.py`) is the live nightly trigger as of 2026-08-29 (fill close above resistance after H2; span365; skip in-channel/RSI 50; `--h2-resist-break-only`; unique-symbol/day, not RS top1); reject L3 shakeout rebuy (`--shakeout-rebuy-bars` stays 0) and ridge P&L confidence sizing on RS-top1; reject tighter max-age, max-wait caps, and bb%B 0.2 (2020-21 fail); squeeze_mom_rising is a small-n overlay not a hard gate); daily long-history defaults to windowed scan (504/252 via `channel_touch_scale.apply_daily_long_history_defaults`; `--no-window-scan` to disable) because v1 `max_low_pivots=16` on a full panel only sees recent swings (caused 0 pre-2020 buys even with IB prefix); separate 15m scaled hunt via `--preset 15m` (`utils/research/channel_touch_scale.py`; not a daily drop-in; not live/nightly). 2026-08-29 300-name full history: `--entry-mode l3_touch --min-l3-wait-bars 12` without `--max-beyond-width 0.25` is **n=1751 E +0.32% PF 1.58** vs prior pivot n=1764 E +0.15 PF 1.24; copying daily beyond-0.25 onto 15m L3 hurts (2020-21 fails); `--entry-touch 4` (L4 after leave-rail) has fewer signals not more (n=1312 E +0.20 PF 1.36, 2022-23 flat); do not promote `min_close_loc` on wick fills (same-bar close; fill-at-close honesty kills PF; lagged prior-bar `min_close_loc` 0.6 also dies E +0.06) or the time-split logistic/MLP (walk-forward lift was the leak; lagged-feature logistic is a mild raw lift, not a hard gate); `--intraday-fill 15m` daily hybrid (first IB 15m tag, skip no-15m names) did not beat same-300 daily wait-6/RSI-50 (n=260 E +1.31 PF 1.47 vs n=314 E +1.73 PF 1.63) — do not wire nightly; 15m H2 resist-break wait-12 + span<=10 + RS top1 **n=1664 E +0.46 PF 1.87** beats 15m L3 wait-12 n=1751 E +0.31 PF 1.57 on the 300 ADV sample (research only, not live); **full IB 15m reverses** — H2 span10+RS n=1766 E +0.30 PF 1.48 vs L3 RS n=1775 E +0.34 PF 1.61 (do not promote 15m H2); 15m unique-symbol H5 + overshoot p80 + vol>=2 is **n=6306 E +0.93% PF 3.61** (research only, not nightly); interactive reports via `scripts/research/generate_channel_touch_tv_report.py` (SPY overlay must IB-prefix so equity and SPY share the first-trade date — Alpaca-only SPY starts ~2020-07 and skips 2019–mid-2020; same-day RS rank must emit null last + stable row `ord` to match Python — missing RS as -Infinity ranked the wrong names; 15m/intraday trade table shows YYYY-MM-DD HH:MM from `buy_time`/`sell_time` when present — calendar `buy_date` stays the max-entries-per-day key; SPY overlay stays daily; Robustness tab, Run info: git/detector/params); stable HTML: `reports/ascending_channels/current_best/` (`1d_channel_touch.html` live daily H2 resist-break span365, `1d_l3_touch.html` retired L3 keeper, `15m_channel_touch.html` research wait-12 L3, `15m_h2_resist_break.html` 300-name wait-12 span10, `15m_full_h2_resist_break.html` full IB 15m H2, `15m_h5_overshoot_vol.html` research unique-symbol H5 + overshoot p80 + vol>=2, `1d_keeper_plus_h2_resist_break.html` combined re-RS); offline suite `scripts/research/channel_touch_robustness.py`; reports under `reports/ascending_channels/`; status under `docs/status_log/edge_hunt/channel_touch/`; full-universe runs use parquet OHLCV cache + parallel workers (TimescaleDB cold load is the bottleneck); do not promote `entry_mode=reclaim` until pivot-confirmation look-ahead is fixed; live daily monitor via CronRunner job `channel_touch_nightly` (`scripts/scanners/channel_touch_nightly.py`); 15m research loop (armed H2 watchlist + Alpaca last at/above resist + bar-close H5, not nightly) via CronRunner jobs `channel_touch_15m` + `channel_touch_15m_proximity` (`scripts/scanners/channel_touch_15m.py`) — playbook `docs/features/channel_touch_15m_live.md`; source of truth is TimescaleDB `channel_touch_15m_candidates` + dashboard `/hot` (JSON watchlist is a debug sidecar); Telegram on H5 fills / newly-hot from cron settings, not TradingView alerts; logs under `logs/scanners/`; nightly daily refresh defaults to Alpaca multi-symbol batch (`fetch_many_from_alpaca` / `--multi-symbol`, batch ~100); TV rails + open-ended support alerts (daily watchlist drawings, not the 15m live path): follow `docs/features/tv_channel_trendline_alert.md` (rule `.cursor/rules/tv_channel_trendline_alert.mdc`); geometry in `reports/ascending_channels/watchlist_channels_draw.json`; use drawing `create_alert` POST (not flat MCP `alert_create`); Pine CTD overlay stacks historical first-valid channels and is not the watchlist teal/red alert rays, nor the trade's H2-L3 rails (visual mismatch vs backtest `channel_start`/`h2_date` is expected); when redrawing long multi-year detector channels, verify left timestamps — TV can snap left endpoints forward and float rails under recent candles
- Largest-breakouts research report: `scripts/research/largest_breakouts_report.py`; outputs under `reports/largest_breakouts/`
